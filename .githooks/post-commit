#!/bin/zsh
set -euo pipefail

if [[ "${DEVSPACE_AUTO_PUBLISH:-1}" != "1" ]]; then
  exit 0
fi

ROOT="$(git rev-parse --show-toplevel)"
for variable in $(git rev-parse --local-env-vars); do
  unset "$variable"
done
zsh "$ROOT/scripts/auto-publish-local.sh"
